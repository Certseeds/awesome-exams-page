# CS109 2025秋 期末

``` meta
Introduction to Computer Programming (CS109) 回忆版

Exam Duration: 2 hours
```

> 温馨提示: 本回忆版由多位同学在2025Fall Semester Final Exam后回忆拼凑而成，仅供参考交流，可能存在错漏之处，无答案

| Question No. | 1  | 2  | 3  | 4  | 5  |
| ------------ | -- | -- | -- | -- | -- |
| Score        | 20 | 20 | 20 | 30 | 10 |

## Part I. True or False

10 × 2 points = 20 points

Please determine whether each statement is true (T) or false (F) and answer T/F accordingly.

### 1-1

`float` has higher precision than `double`.

### 1-2

In Java, an int value can be implicitly promoted to a `double` value without explicit type cast.

### 1-3

A `for` loop header must always include initialization, condition, and update expressions.

### 1-4

A `do-while` loop checks its condition before the first iteration.

### 1-5

Constructors don't have a return `type`.

### 1-6

A `switch` expression must evaluate to an `int` in Java.

### 1-7

All classes in Java inherit from the `Object` class by default.

### 1-8

Arrays in Java are passed to methods by value, but the value is a reference.

### 1-9

Polymorphism allows a child reference to hold a parent object.

### 1-10

An interface can implement multiple other interfaces.

## Part II. Fill in the Blanks

9 questions (10 blanks) × 2 points = 20 points

Fill in the blanks below using the most appropriate word from the following list. You may change the forms of the words as needed to follow English grammar rules.

Word list:

abstract, class, enum, extends, final, for, foreach (i.e., enhanced for), while, if, generics, instance, implements, overload, override, overwrite, private, package-private, package, protected, public, static, switch, super, this

### 2-1

If you want to define a fixed set of named constants, such as the days of the week, you should use the $\underline{\quad\quad\quad}$ keyword.

### 2-2

When a subclass provides a specific implementation for a method that is already defined in its parent class, this is known as method $\underline{\quad\quad\quad}$.

### 2-3

The $\underline{\quad\quad\quad}$ access modifier makes a member accessible within its own package and by subclasses, even if the subclass is in a different package.

### 2-4

An $\underline{\quad\quad\quad}$ class is a restricted class that cannot be used to create objects. To access members in such a class, it must be inherited from another class.

### 2-5

When iterating through elements of an array or a collection without needing to know the index, the $\underline{\quad\quad\quad}$ loop is often the most concise choice.

### 2-6

A class in Java uses the keyword $\underline{\quad\quad\quad}$ to inherit from a super class, whereas it uses the keyword $\underline{\quad\quad\quad}$ to inherit from an interface.

### 2-7

The keyword $\underline{\quad\quad\quad}$ in methods is used to refer to the specific object that is calling the method, which is helpful when an attribute name is the same as a parameter name.

### 2-8

$\underline{\quad\quad\quad}$ allow you to define classes, interfaces, and methods with type parameters. Such a feature provides compile-time type safety and eliminates the need for class type casting.

### 2-9

If no access modifier is specified for a member, it defaults to $\underline{\quad\quad\quad}$ access, which means it is only visible to other classes within the same package folder structure.

## Part III. Multiple-Choice Questions

10 × 2 points = 20 points

> [!NOTE]
>
> Only one choice is correct.

### 3-1

Which one of the following identifiers is valid in Java?

+ A. 123xyz
+ B. `_constant_`
+ C. `a*`
+ D. enum

### 3-2

Which variable is always accessible within all methods of a class?

+ A. A local variable
+ B. A method parameter
+ C. An instance variable
+ D. A block-scoped variable

### 3-3

What best describes the relation between `if` and `switch`?

+ A. `switch` can represent all conditional logic that `if` can express.
+ B. `switch` supports ranges; `if` does not.
+ C. `if` is easier to read than `switch`.
+ D. `switch` is better suited when choosing among several fixed, discrete outcomes of one value or expression.

### 3-4

Which of the following is true about abstract methods?

+ A. They describe what should be done but not how it is done.
+ B. They must include a default implementation.
+ C. They can be executed directly after instantiating the abstract class.
+ D. They cannot participate in polymorphism.

### 3-5

Which statement is true about instance variables?

+ A. They must be explicitly initialized when declared.
+ B. They can be overridden by subclasses.
+ C. Their values are shared across all instances of a class.
+ D. They are created when an object is instantiated.

### 3-6

Which of the following statements about `String` is true?

+ A. Two `String` objects with identical characters must always reference the same memory location.
+ B. `String` objects are immutable, meaning their internal character data never changes after creation.
+ C. The key difference between `String` and `StringBuilder` is that `String` is `JDK` built-in while `StringBuilder` is not.
+ D. The `String` class can be extended to add new behaviors in user-defined subclasses.

### 3-7

Which one of the following statements is false?

+ A. A `private` method cannot be overridden by subclasses.
+ B. A `public` method can be accessed from anywhere in the program.
+ C. The primary purpose of access modifiers is to control visibility and enforce encapsulation boundaries.
+ D. Only `public` access is allowed for top-level classes.

### 3-8

Which statement about polymorphism is correct?

+ A. Subtypes should be usable wherever their supertypes are expected.
+ B. The method implementation is chosen based on the declared type rather than the actual type.
+ C. Interfaces cannot be used to achieve runtime polymorphic behavior.
+ D. Polymorphism applies to both the fields and methods.

### 3-9

Which statement about the `super` keyword is correct?

+ A. A superclass constructor is invoked only when `super` is explicitly written.
+ B. `super` cannot be used in a static context.
+ C. `super` can access all members of the superclass regardless of access level.
+ D. `super()` can be used anywhere inside a constructor.

### 3-10

What is the difference between abstract classes and interfaces?

+ A. Interfaces can have constructors but abstract classes cannot have constructors.
+ B. A class can extend multiple abstract classes but cannot implement multiple interfaces.
+ C. Interfaces can capture similarity between unrelated objects without forcing a class inheritance relationship.
+ D. Interfaces can have instance variables but abstract classes cannot.

## Part IV. Program Output

10 × 3 points = 30 points

Write the output of each of the following code snippets. Assume that the code for each question is in the same .java file and that all code compiles successfully.

### 4-1

Compile and run the following program with the following command line invocation:

``` text
> java Q1 5.5 2 hello
```

``` java
public class Q1 {
    public static void main(String[] args) {
        double a = Double.parseDouble(args[0]);
        int b = Integer.parseInt(args[1]);
        String s = args[2];
        double result = a / b + s.length();
        String output = s + result;
        System.out.println(output);
    }
}
```

Program output: $\underline{\quad\quad\quad}$

### 4-2

``` java
public class Q2 {
    public static void main(String[] args) {
        int a, b, c, d;
        a = 1; b = 2; c = 3; d = 4;
        int e = (a + 1 < b) ? (c+1 / b) : d * c;
        System.out.println(e);
    }
}
```

Program output: $\underline{\quad\quad\quad}$

### 4-3

``` java
public class Q3 {
    public static void main(String[] args) {
        int sum = 0;
        int i = 1;
        for (; i <= 6; i++) {
            if (i == 2 || i == 4) {
                continue;
            }
            sum = sum + i;
            if (sum > 7) {
                break;
            }
        }
        System.out.println(sum + i);
    }
}
```

Program output: $\underline{\quad\quad\quad}$

### 4-4

``` java
public class Q4 {
    public static void main(String[] args) {
        String s = "Java";
        String t = s;
        s.concat(" Language");
        t = t + " Language";
        if (s.equals(t)) {
            System.out.println(t);
        } else {
            System.out.println(s);
        }
    }
}
```

Program output: $\underline{\quad\quad\quad}$

### 4-5

``` java
public class Q5 {
    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4, 5};
        for (int i = arr.length - 1; i > 0; i--) {
            arr[i] += arr[i - 1];
        }
        for (int num : arr) {
            System.out.print(num + " ");
        }
    }
}
```

Program output: $\underline{\quad\quad\quad}$

### 4-6

``` java
public class Q6 {
    public static void main(String[] args) {
        int x = 10;
        int[] arr = {1, 2, 3};
        change(x, arr);
        System.out.println(x + arr[0]);
    }

    public static void change(int a, int[] b) {
        a += 5;
        b[0] += 5;
    }
}
```

Program output: $\underline{\quad\quad\quad}$

---

### 4-7

``` java
public class Q7 {
    public static int value = 1;
    public int count = 1;

    public Q7() {
        count++;
        value += 5;
    }

    public static void main(String[] args) {
        Q7 t1 = new Q7();
        Q7 t2 = new Q7();
        System.out.printf("%d,%d,%d", Q7.value, t1.count, t2.count);
    }
}
```

Program output: $\underline{\quad\quad\quad}$

### 4-8

``` java
class A {
    int a;

    public int foo() {
        return a + 10;
    }

    public static int foo(int a) {
        return a + 20;
    }
}

public class Q8 extends A {
    public int foo() {
        return a - 1;
    }

    public static int foo(int a) {
        return a - 2;
    }

    public static void main(String[] args) {
        A x = new Q8();
        int m = x.foo(8);
        int n = x.foo();
        System.out.println(m + n);
    }
}
```

Program output: $\underline{\quad\quad\quad}$

### 4-9

``` java
class Parent {
    String name = "A";

    Parent() {
        name = "B";
    }

    String getName() {return name;}
}

class Child extends Parent {
    String name = "C";

    String getName() {
        return super.getName() + super.name + "-" + name;
    }
}

public class Q9 {
    public static void main(String[] args) {
        Parent p = new Child();
        System.out.println(p.getName());
    }
}
```

Program output: $\underline{\quad\quad\quad}$

### 4-10

``` java
class P {
    P() {
        System.out.print("P");
    }

    void print() {
        System.out.print("x");
    }
}

class Q extends P {
    void print() {
        System.out.print("y");
    }
}

class R extends Q {
    R() {
        System.out.print("R");
    }
}

public class Q10 {
    public static void main(String[] args) {
        P[] arr = {new P(), new Q(), new R()};
        for (P p : arr) {
            p.print();
        }
    }
}
```

Program output: $\underline{\quad\quad\quad}$

## Part V. Programming

10 points

### 5-1 Fill in the Blanks of the Class `CinemaHall`

7 points, 6 blanks in total

In a cinema ticketing system, we need to track the seating status of a movie hall. We use a two-dimensional array to represent the hall, where the rows represent the physical rows in the theater, and the columns represent the seat numbers in that row. Each element in the array stores an integer code representing the ticket type sold for that seat: 0 for empty, 1 for a Regular Ticket (¥35), and 2 for a VIP Ticket (¥60).

Your task is to complete the `CinemaHall` class. The class must allow booking seats, calculating the total money earned by the hall, i.e., the revenue, and comparing two halls to see which one has generated more money.

+ Blank 1: `CinemaHall` implements the `compareTo` method from the standard Java library to support sorting or comparison based on revenue.
+ Blank 2: Check if the input dimensions (numRows, numSeats) are in the allowed limits. If the input exceeds MAX_ROWS or MAX_SEATS, or if they are non-positive, print "Error: Invalid dimensions" and force the hall size to be a default of 10 rows by 10 seats.
+ Blank 3: Initialize the 2D array seats.

  > [!NOTE]
  >
  > The first dimension corresponds to the number of rows, and the second dimension corresponds to the number of seats per row.

+ Blank 4: Complete the validation condition for the bookSeat method. The row and seat indices must be within the array bounds. Additionally, the ticketType must be a valid code: it can only be 0 Empty, 1 Regular, or 2 VIP.
+ Blank 5: Write the method `calculateTotalRevenue`. This method should iterate through the entire seating grid and calculate the total money earned, i.e., total revenue. Remember: Type 0 contributes ¥0, Type 1 adds PRICE_REGULAR, and Type 2 adds PRICE_VIP.
+ Blank 6: Implement the `compareTo` method. It should compare the current object with another `CinemaHall` object based on their total revenue values. Return 1 if the current hall has higher revenue, -1 if lower, and 0 if they are equal.

``` java
public class CinemaHall _________Blank_1_(1_point)___________ {
    // Static constants for limits and prices
    public static final int MAX_ROWS = 50;
    public static final int MAX_SEATS = 50;
    public static final int PRICE_REGULAR = 35;
    public static final int PRICE_VIP = 60;

    // Attributes
    private int[][] seats; // Values: 0=Empty, 1=Regular, 2=VIP
    private int numRows;
    private int numSeats;

    // Constructor
    public CinemaHall(int numRows, int numSeats) {
        if (numRows <= 0 || numRows > MAX_ROWS ||
                numSeats <= 0 || numSeats > MAX_SEATS) {
            ___Blank_2_(1_point,and_you_may_need_to_write_multiple_lines_of_code_here)__
        }
        this.numRows = numRows;
        this.numSeats = numSeats;
        this.seats = ______Blank_3_(1_point)______________;
        // Initialize the array
    }

    // Method to update a seat status
    public void bookSeat(int row, int seat, int ticketType) {
        // Check bounds AND check if ticketType is 0, 1, or 2
        if ( _____Blank_4_(1_point)_______________ ) {
            seats[row][seat] = ticketType;
        } else {
            System.out.println("Invalid seat or ticket type.");
        }
    }

    // Method to calculate total revenue
    public int calculateTotalRevenue() {
        _Blank_5_(1_point,and_you_may_need_to_write_multiple_lines_of_code_here)
    }

    @Override
    public int compareTo(CinemaHall other) {
        _Blank_6_(2_point,and_you_may_need_to_write_multiple_lines_of_code_here)
    }
}
```

### 5-2 Group Booking with the Best View Quality

3 points

Continuing with the previous problem, consider a single row of seats in the cinema hall. Not all seats provide the same viewing experience. Seats in the center of the row generally offer a better view than seats at the far ends or directly behind a pillar. We can represent this by assigning an integer "viewing quality score" to each seat in the row sequentially. Some scores might be negative if the view is particularly bad.

A group of `k` friends wants to book tickets to watch a movie together. To ensure they can sit next to each other, they must book `k` consecutive seats in that row. The goal is to find a block of `k` consecutive seats such that the total sum of their viewing quality scores is maximized.

Please complete the static method `findMaxGroupScore` with the following two arguments:

+ An integer array `seatScores`, representing the viewing quality scores of the seats in the row in order;
+ An integer k, representing the number of friends in the group, i.e., the size of the consecutive block needed. You can assume that `k` is always greater than 0 and less than or equal to the total number of seats (`seatScores.length`).

The method should return an integer representing the **maximum total quality** score obtainable by selecting exactly `k` consecutive seats.

``` java
public static int findMaxGroupScore(int[] seatScores, int k) {
    int n = seatScores.length; // Total number of seats in the row
    int maxScore = Integer.MIN_VALUE;
    // The maximum total quality score obtainable
    ______Blank_here_(3_points)_______________
    // Write your code to find the maximum sum of k consecutive elements.
    return maxScore;
}
```

> Hint:
>
> Both brute-force solutions using nested loops and more efficient solutions such as using the sliding window technique are acceptable to receive full marks.
