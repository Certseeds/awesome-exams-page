# CS109 2023春 期末

``` meta
CS109 计算机编程 期末 2023春 (回忆版)
```

## Part I: Judgments

Determine whether the following statements about Java are True (T) or False (F).

### 1-1

$\underline{\quad\quad\quad}$ Assignment operator is evaluated from left to right.

### 1-2

$\underline{\quad\quad\quad}$ Consider the statement x = (a>b) ? a:b ;. The value of x is 27 if a = 18 and b = 27.

### 1-3

$\underline{\quad\quad\quad}$ The expression (y >= z && a == b) is evaluated by first evaluating the expression y >= z, and then evaluating a == b.

### 1-4

$\underline{\quad\quad\quad}$ The switch selection structure must end with the default case.

### 1-5

$\underline{\quad\quad\quad}$ An individual array element from an array of type int, when passed to a method, is pass-by-value.

### 1-6

$\underline{\quad\quad\quad}$ The == operator can be used to compare two String objects. The result is true if the two strings are identical.

### 1-7

$\underline{\quad\quad\quad}$ A method in a class declared as static is invoked by using the name of the method alone.

### 1-8

$\underline{\quad\quad\quad}$ Each method in a class must have a unique name.

### 1-9

$\underline{\quad\quad\quad}$ A method that is overridden in the subclass must retain the same return type and parameter list.

### 1-10

$\underline{\quad\quad\quad}$ The access level of an overridden method must be identical in a parent class and its subclass.

### 1-11

$\underline{\quad\quad\quad}$ When you run Child.staticMethod(), if the Child class extends Parent and does not have the static method staticMethod, Java will try to invoke Parent.staticMethod().

### 1-12

$\underline{\quad\quad\quad}$ A subclass inherits only the non-private members of its superclass.

### 1-13

$\underline{\quad\quad\quad}$ A subclass cannot get the value of non-private members of its superclass by any means.

### 1-14

$\underline{\quad\quad\quad}$ All methods in a final class must be explicitly declared final.

### 1-15

$\underline{\quad\quad\quad}$ Objects of a subclass can always be assigned to a superclass variable.

## Part II: Fill in the Blank

Fill in the blanks below using the most appropriate keyword or terminology from the following list: abstract, break, class, continue, default, double, enum, extends, final, implements, import, instanceof, interface, int, long, new, null, overload, override, private, protected, public, return, static, super, this, try, void, while. These words may be used in multiple blanks. Misspelled words are considered wrong.

### 2-1

The $\underline{\quad\quad\quad}$ keyword is an access modifier used for attributes, methods and constructors making them accessible in the same package and subclasses.

### 2-2

$\underline{\quad\quad\quad}$ is a Java keyword that is used to indicate that the method does not have a return type, i.e., it will not be returning any value.

### 2-3

If you use a(n) $\underline{\quad\quad\quad}$ keyword with a variable or a method inside a class, then for every instance that you create for that class, you can't redefine these members. These instances all have the members, but the variable value can be changed.

### 2-4

On implementation of a(n) $\underline{\quad\quad\quad}$, you must override all of its methods.

### 2-5

Using $\underline{\quad\quad\quad}$, we can force immediate termination of a loop, bypassing the conditional expression and any remaining code in the body of the loop.

### 2-6

To get an int, you need to divide a(n) $\underline{\quad\quad\quad}$ by a(n) $\underline{\quad\quad\quad}$.

### 2-7

Two or more methods may have the same name if they differ in parameters. These methods are called $\underline{\quad\quad\quad}$ method.

### 2-8

You cannot instantiate a(n) $\underline{\quad\quad\quad}$ class or a(n) $\underline{\quad\quad\quad}$.

### 2-9

The $\underline{\quad\quad\quad}$ keyword is a literal that represents a reference that does not refer to any object.

### 2-10

The $\underline{\quad\quad\quad}$ keyword is used to call superclass methods, and to access the superclass constructor.

### 2-11

When a variable is declared with the $\underline{\quad\quad\quad}$ keyword, its value can't be modified, essentially a constant.

### 2-12

The $\underline{\quad\quad\quad}$ keyword is a primitive data type indicating a floating point number, more precise than a float. Since it is primitive, you cannot use keyword $\underline{\quad\quad\quad}$ to instantiate an object of it.

## Part III: Multiple Choice

### 3-1

Suppose a class has public visibility. In this class we want to define a protected method. Which of the following statements is true?

+ A. This method is only accessible from inside the class itself and from inside all subclasses.
+ B. In a class, you cannot declare methods with a lower visibility than the visibility of the class in which it is defined.
+ C. From within protected methods you do not have access to public methods.
+ D. This method is accessible from within the class itself and from within all classes defined in the same package as the class itself.

$\underline{\quad\quad\quad}$

### 3-2

Which of the following statements is true?

+ A. In case of overloading, binding of objects with methods happens at runtime.
+ B. In the case of dynamic polymorphism, method choice is decided at runtime.
+ C. In the case of overriding, the creation of objects happen at compile time and these objects are used for calling objects at runtime.
+ D. In the case of overriding, it is the responsibility of the compiler to bind the method calls with the method body.

$\underline{\quad\quad\quad}$

### 3-3

Among the following statements

a. `Parent p = new Child ();`

b. `Child c = new Parent ();`

c. `List <Parent> list = new ArrayList <> (); list.add (new Child ());`

d. `List <Child> list = new ArrayList <> (); list.add (new Parent ());`

which ones are correct?

+ A. a,b
+ B. a,c
+ C. c,d
+ D. a,c,d

$\underline{\quad\quad\quad}$

### 3-4

Which of the following for loop declaration is not valid?

+ A. `for (int i = 20; i >= 2; --i)`
+ B. `for (int i = 7; i <= 77; i += 7)`
+ C. `for (int i = 99; i >= 0; i / 9)`
+ D. `for (int i = 2; i <= 20; i = 2 * i)`

$\underline{\quad\quad\quad}$

### 3-5

Which option is false about the final keyword?

+ A. A final method cannot be overridden in its subclasses.
+ B. A final class cannot be extended.
+ C. A final class cannot extend other classes.
+ D. A final method can be inherited.

$\underline{\quad\quad\quad}$

### 3-6

Which is the value of total after executing the following code snippet?

``` java
int meal = 5;
int tip = 2;
int total = meal + (meal > 6 ? ++ tip : -- tip);
```

+ A. 1
+ B. 2
+ C. 3
+ D. 6

$\underline{\quad\quad\quad}$

### 3-7

Which one of the following is a valid statement?

+ A. `char [] c = new char (4);`
+ B. `char [] c = new char [5];`
+ C. `char [] c = new char (4);`
+ D. `char [] c = new char [];`

$\underline{\quad\quad\quad}$

### 3-8

Which line of the following contains a compilation error? Why?

``` java
public class Test {
    Test () { }
    static void Test () { this (); }
    public static void main (String [] args) {
        Test ();
    }
}
```

+ A. At line 2, constructor Tester must be marked public like its class.
+ B. At line 3, no constructor calls are allowed inside a method.
+ C. At line 3, this object cannot be referenced from a static method.
+ D. At line 5, keyword new is required to instantiate an object.

$\underline{\quad\quad\quad}$

### 3-9

Suppose A is an abstract class, B is a concrete subclass of A. Both A and B have a default constructor. Among the following statements

a. `A a = new A ();`

b. `A a = new B ();`

c. `B b = new A ();`

d. `B b = new B ();`

which ones are correct?

+ A. a,b
+ B. b,c
+ C. b,d
+ D. c,d

$\underline{\quad\quad\quad}$

### 3-10

Which of the following statements is true?

+ A. X extends Y is correct if and only if X is a class and Y is an interface.
+ B. X extends Y is correct if and only if X is an interface and Y is a class.
+ C. X extends Y is correct if X and Y are either both classes or both interfaces.
+ D. X extends Y is correct for all combinations of X and Y being classes and/or interfaces.

$\underline{\quad\quad\quad}$

## Part IV: Output of Java

Write the output of each of the following code snippets. If running a code snippet would produce an error, describe the problem.

### 4-1

``` java
class Test {
    protected int x, y;
}
class Main {
    public static void main (String args []) {
        Test t = new Test ();
        System.out.println (t.x + " " + t.y);
    }
}
```

$\underline{\quad\quad\quad}$

### 4-2

``` java
public class Test {
    int num = 100;
    public void calc (int num) { this.num = num * 10; }
    public void printNum () { System.out.println (num); }
    public static void main (String [] args) {
        Test obj = new Test ();
        obj.calc (2);
        obj.printNum ();
    }
}
```

$\underline{\quad\quad\quad}$

### 4-3

``` java
class Temp {
    private Temp (int data) { System.out.printf ("Constructor called "); }
    protected static Temp create (int data) {
        Temp obj = new Temp (data);
        return obj;
    }
    public void myMethod () { System.out.printf ("Method called"); }
}
public class Test {
    public static void main (String [] args) {
        Temp obj = Temp.create (20);
        obj.myMethod ();
    }
}
```

$\underline{\quad\quad\quad}$

### 4-4

``` java
class Base {
    final public int calc (int a, int b) { return 0; }
}
class Derived extends Base {
    public int calc (int a, int b) { return 1; }
}
public class Calculator {
    public static void main (String args []) {
        Derived get = new Derived ();
        System.out.println ("x = " + get.calc (0,1));
    }
}
```

$\underline{\quad\quad\quad}$

### 4-5

``` java
public class Test {
    public static void main (String [] args) {
        String a = args [1];
        String b = args [2];
        String c = args [3];
        System.out.println ("output = " + c);
    }
}
```

And execute the program using command java Test a b c.

$\underline{\quad\quad\quad}$

### 4-6

``` java
public class Test {
    public static void main (String [] args) {
        int i = 1;
        switch (i) {
            case 0:
                System.out.print (0);
            case 1:
                System.out.print (1);
            case 2:
                System.out.print (2);
            default:
                System.out.print (-1);
        }
    }
}
```

$\underline{\quad\quad\quad}$

### 4-7

``` java
public class Test {
    public static void main (String [] args) {
        int a = 0;
        int b = 0;
        for (int i = 0; i < 4; i++) {
            if ((a++ > 2) || (b++ > 2)) {
                a++;
            }
        }
        System.out.println (a + " " + b);
    }
}
```

$\underline{\quad\quad\quad}$

### 4-8

``` java
public class Test {
    public static void main (String [] args) {
        int a = 52;
        double b = 52.3;
        boolean c = (a = b);
        System.out.println (c);
    }
}
```

$\underline{\quad\quad\quad}$

### 4-9

``` java
class Base {
    public void Print () { System.out.print ("Base"); }
}
class Derived extends Base {
    public void Print () { System.out.print ("Derived"); }
}
class Main {
    public static void DoPrint (Base o) { o.Print (); }
    public static void main (String [] args) {
        Base x = new Base ();
        Base y = new Derived ();
        Derived z = new Derived ();
        DoPrint (x);
        DoPrint (y);
        DoPrint (z);
    }
}
```

$\underline{\quad\quad\quad}$

### 4-10

``` java
abstract class Abst {
    public int a;
    Abst () { a = 10; }
    abstract public void set ();
    abstract final public void get ();
}
class Test extends Abst {
    public void set (int a) { this.a = a; }
    final public void get () { System.out.println ("a = " + a); }
    public static void main (String [] args) {
        Test obj = new Test ();
        obj.set (20);
        obj.get ();
    }
}
```

$\underline{\quad\quad\quad}$

## Part V: Programming

### 5-1

Write no more than eight Java statements in the highlighted blank to check if all the characters in a String are unique, i.e., each character appears only once.

``` java
public class StringAllUniqueChar {
    public static void main (String [] args) {
        System.out.println("CS107 has all unique chars : " + hasAllUniqueChars ("Cs107"));
        System.out.println("Introduction has all unique chars : " + hasAllUniqueChars ("Introduction"));
        System.out.println("to has all unique chars : " + hasAllUniqueChars ("to"));
        System.out.println("Computer has all unique chars : " + hasAllUniqueChars ("Computer"));
        System.out.println("Programming has all unique chars : " + hasAllUniqueChars ("Programming"));
    }

    public static boolean hasAllUniqueChars (String word) {
        // No more than eight Java statements here.
    }
}
```

This code block should produce the following result:

``` text
CS107 has all unique chars : true
Introduction has all unique chars : false
to has all unique chars : true
Computer has all unique chars : true
Programming has all unique chars : false
```

### 5-2

You need to design a class named Triangle to represent different triangles. Your Triangle class should have three double instance variables a,b,c to represent the three sides of a triangle. You need to implement a constructor that validates whether the three sides can form a valid triangle (the sum of the lengths of any two sides of a triangle has to be greater than the length of the third side). If yes, instantiate a new Triangle object with the three sides; otherwise, no need to do anything. Your Triangle class should also implement the interface java.lang.Comparable `<T>` to compare two triangles using their areas. A method area() to calculate the area of the triangle is given in the template code.

``` java
public class Triangle /* What should be here ?? */ {
    private double a,b,c;

    public Triangle (double a, double b, double c) {
        // No more than five Java statements here.
    }

    public double area () {
        double p = (a+b+c) / 2;
        return Math.sqrt (p * (p-a) * (p-b) * (p-c));
    }

    @Override
    public int compareTo (Triangle t) {
        if (/* What should be here? */) {
            return -1;
        } else if (/* What should be here? */) {
            return 1;
        } else {
            return 0;
        }
    }
}
```

### 5-3

(Bonus) You are given a one dimensional array that may contain both positive and negative integers (no less than -10), find the sum of contiguous subarray of numbers which has the largest sum. For example, if the given array is {-2, -5, 6, -2, -3, 1, 5, 6}, then the maximum subarray sum is 13[^0]. Write no more than ten Java statements in the highlighted blank to fulfill the requirement.

``` java
public class LargestContiguousSubarraySum {
    public static void main (String[] args) {
        int [] arr = {-2, -5, 6, -2, -3, 1, 5, 6};
        System.out.println ("The largest sum of contiguous subarray is " + findLargestSum(arr));
    }

    public static int findLargestSum (int [] arr) {
        // No more than ten Java statements here.
    }
}
```

This code block should produce the following result:

``` text
The largest sum of contiguous subarray is 13
```

### 5-4

(Bonus) A naive solution to the previous question may involve multiple nested for loops, which leads to a high computation complexity. Try to write an advanced solution with only one for loop and no recursions. Explain your algorithm.

[^0]: 校者注, 原始为7, 疑似是原题干的解答
